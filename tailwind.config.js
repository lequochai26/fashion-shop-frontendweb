module.exports = {
    content: ["./src/**/*.{js,jsx,ts,tsx}"], // Adjust paths as needed
    theme: {
      extend: {}, // Add custom Tailwind CSS classes here
    },
    plugins: [], // Add any Tailwind plugins if needed
  };